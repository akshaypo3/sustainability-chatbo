apiVersion: ai.sap.com/v1alpha1
kind: Configuration
metadata:
  name: chatbot-config   # 👈 this is the config name you will reference in deployment
spec:
  parameters:
    # Example: environment variables you want to pass at runtime
    GEMINI_MODEL: "gemini-pro"   # 👈 since you are using Gemini, you can set model name here
    MAX_TOKENS: "512"
    TEMPERATURE: "0.7"
